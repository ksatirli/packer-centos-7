---

- name: prepping Ansible Control for Packer
  gather_facts: false
  hosts: localhost

  tasks:
    - name: installing Ansible role(s)
      command: >
        ansible-galaxy
          --roles-path="./files/playbooks/roles/"
          --role-file="./files/vars/ansible-roles.yml"
          install
